name: ci
on:
  pull_request:
    branches:
      - main
      - dev

env:
  CLANG_TIDY_VERSION: "15.0.2"
  VERBOSE: 1

jobs:
  Test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false

      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        compiler:
          - gcc-11
          - llvm-12
        generator:
          - "Ninja"
        build_type:
          - Release
          - Debug

    steps:
      - uses: actions/checkout@v3

      ## install scoop for windows and install xmake
      - name: Install scoop
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          iex (new-object net.webclient).downloadstring('https://get.scoop.sh')
          scoop install xmake

      - name: Install xmake
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          sudo add-apt-repository ppa:xmake-io/xmake
          sudo apt update
          sudo apt install xmake

      - name: Install xmake
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          brew install xmake
          xmake --version

      - name: Setup Cache
        uses: ./.github/actions/setup_cache
        with:
          compiler: ${{ matrix.compiler }}
          build_type: ${{ matrix.build_type }}
          packaging_maintainer_mode: ${{ matrix.packaging_maintainer_mode }}
          generator: ${{ matrix.generator }}

      - name: Setup Cpp
        uses: aminya/setup-cpp@v1
        with:
          compiler: ${{ matrix.compiler }}
          make: true
          ccache: true
          clangtidy: ${{ env.CLANG_TIDY_VERSION }}
          cppcheck: true

      - name: Configure Xmake
        run: |
          xmake f -p ${{matrix.os}} -m ${{matrix.build_type}} --verbose -y

      - name: Build Xmake
        run: |
          xmake -v -a ${{matrix.arch}} -j 4

      - name: Unix - Test and coverage
        working-directory: ./build
        run: |
          xmake run test
